/** Tick interval for the footer's "X min ago" label (1 minute). */
export const FOOTER_AGE_TICK_MS = 60_000;

/** Splash screen logo size (px, square). */
export const SPLASH_LOGO_PX = 120;

/** Splash progress bar width (px). */
export const SPLASH_PROGRESS_WIDTH_PX = 240;
